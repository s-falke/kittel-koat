var x: int, y: int, z: int;

begin
  while ((x > z) or (y > z)) do
    if (x > z) then
      x = x - 1;
    else
      if (y > z) then
        y = y - 1;
      else
        skip;
      endif;
    endif;
  done;
end
