var x: int, tmp: int;

begin
  while (x > 0) do
    tmp = random;
    if (brandom) then
      assume (x == 2 * tmp);
      x = x + 1;
    else
      assume (x == 2 * tmp + 1);
      x = x - 3;
    endif;
  done;
end
