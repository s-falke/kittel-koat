var x: int, y: int;

begin
  while ((x > 0) or (y > 0)) do
    if (x > 0) then
      x = x - 1;
    else
      if (y > 0) then
        y = y - 1;
      else
        skip;
      endif;
    endif;
  done;
end
