var x: int, y: int, flag: int;

begin
  flag = 1;
  while (flag > 0) do
    if (x >= 0) then
      x = x - 1;
      y = random;
    else
      if (y >= 0) then
        y = y - 1;
      else
        flag = 0;
      endif;
    endif;
  done;
end
